// select stories for storybook

import React from 'react';
import { ComponentStory, ComponentMeta } from '@storybook/react';
import { Select } from '@rotational/beacon-core';

export default {
  title: 'beacon/Select',
  component: Select,
} as ComponentMeta<typeof Select>;
